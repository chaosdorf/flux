apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: labello
spec:
  rules:
    - http:
        host: labello.apps.chaosdorf.space
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: labello
                port:
                  number: 80
