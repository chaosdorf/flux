apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: minio
spec:
  interval: 1h
  chart:
    spec:
      chart: operator
      sourceRef:
        kind: HelmRepository
        name: minio
      interval: 1h
